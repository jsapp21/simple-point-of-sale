const initialState = {
    category: []

};

export const productReducer = (state = initialState, action) => {
    switch (action.type) {
        case 'ADD_PRODUCT':
            return state
        default:
            return state
    }
};